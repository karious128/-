# 发票识别与智能分析系统实验报告

## （1）项目简介

### 选题说明
在企业财务管理和个人报销场景中，人工录入发票信息不仅效率低下，且极易出错。随着 OCR（光学字符识别）技术和大模型（LLM）的发展，自动化识别发票已成为企业数字化转型的标配。本项目旨在利用 Python 构建一个端到端的发票识别系统，实现从图片上传、信息提取到数据可视化分析的全流程自动化。

### 功能描述
* **智能上传**：支持批量上传多种格式（JPG/PNG）的发票图片。
* **多维度识别**：自动提取发票类型、开票单位、受票单位、总金额、税率及日期。
* **图像预处理**：内置图像灰度化、对比度增强与锐化功能，提升复杂背景下的识别精度。
* **实时校对**：提供可编辑的数据表格，允许财务人员对识别结果进行人工微调。
* **数据可视化**：自动生成报销金额统计、供应商支出分布柱状图及发票类型占比饼图。
* **数据导出**：支持一键导出符合财务标准的 CSV 汇总报表。

---

## （2）系统设计

### 架构图
该系统采用经典的“前端展示-后端逻辑-AI引擎”三层架构：
1.  **表现层 (Streamlit)**：负责 Web 交互、文件上传及结果的可视化呈现。
2.  **业务逻辑层 (Pandas & PIL)**：负责数据结构化处理、Session 状态管理及图像增强。
3.  **核心引擎层 (Tesseract OCR)**：负责底层光学字符识别及正则语义提取。

### 技术选型
* **开发语言**：Python 3.13
* **前端框架**：Streamlit (轻量级 Web 开发框架)
* **OCR 引擎**：Tesseract-OCR (开源、支持多语言识别)
* **图像处理**：Pillow (PIL 派生版)
* **数据处理**：Pandas (用于数据汇总与导出)
* **可视化**：Vega-Lite (集成于 Streamlit 的绘图引擎)

---

## （3）AI 使用过程（重点）

### 对话记录
本系统的开发采用了 **“人机协作”模式**：
1.  **初期需求阶段**：向 AI 描述功能需求（上传、OCR、统计），由 AI 生成初步的技术方案。
![alt text](data/AI1.png)

AI:要构建一个高效、丰富的发票识别与分析系统，可以采用“**大模型（LLM）+ 多模态 OCR + 后端数据管理**”的架构方案。以下是为您设计的系统功能规划及技术实现建议：
### 1. 系统核心功能设计
* **智能上传与预处理**：支持手机拍照、扫描件、PDF电子发票批量上传。系统自动进行图像旋转校正、去噪和暗角增强，确保识别准确度。
* **多模态信息提取**：
* **基础字段**：开票单位、受票单位（名称及税号）、开票日期、发票号码、校验码。
* **金额明细**：含税金额、不含税金额、合计税额、具体税率（税点）。
* **类型识别**：自动区分增值税专用发票、普通发票、行程单、医疗单据等。
* **真伪自动校验**：接入国税总局查验接口，自动比对发票信息，防止虚假开票或重复报销。
* **可视化数据统计**：
* **按单位汇总**：统计与各供应商或客户的交易总额。
* **消费分布**：按类目（如交通、餐饮、服务）生成饼图或柱状图。
* **月度趋势**：展示费用支出的月度波动情况。
---

### 2. 技术架构推荐

为了达到“功能丰富、使用便捷”的要求，建议采用以下技术栈：
* **前端**：React 或 Vue.js（Web端），或者使用 Uni-app / 微信小程序（方便手机随时拍照上传）。
* **后端**：Python (FastAPI 或 Flask)，便于集成 AI 模型。
* **AI 识别层**（核心）：
* **方案 A（高精度、低开发量）**：调用云端 API（如 Google Cloud Vision OCR 或 百度 OCR）。它们对增值税发票有专项优化，结构化提取字段非常成熟。
* **方案 B（大模型驱动）**：使用 **GPT-4o** 或 **Claude 3.5 Sonnet**。直接通过 Prompt（提示词）让模型返回 JSON 格式数据。
* *示例 Prompt*：“请识别这张图片中的发票信息，提取开票单位、金额、税率，并以 JSON 格式输出。”
* **数据库**：PostgreSQL 或 MySQL，用于存储结构化发票数据及统计结果。
---
### 3. 数据分析与展示逻辑
系统应提供一个直观的后台看板（Dashboard）：
* **数据汇总表**：支持导出 Excel，方便财务直接对账。
* **自动分类**：利用大模型对发票内容进行语义分类（例如：识别到“酒店”自动归类为“差旅费”）。
* **异常提醒**：如果同一张发票被重复上传，系统应自动拦截并提示。
---
### 4. 获取与开发建议
如果您希望**快速搭建**：
1. **低代码平台**：可以使用腾讯云微搭或宜搭，集成现成的 OCR 插件。
2. **开源参考**：在 GitHub 上搜索 `invoice-ocr` 或 `easyocr`，可以找到许多成熟的 Python 开源框架进行二次开发。


2.  **代码实现阶段**：由 AI 提供基于 Streamlit 和 Tesseract 的核心代码框架。
![alt text](data/AI2.png)   

3.  **Debug 阶段**：将运行中出现的 `ModuleNotFoundError`(缺少模块)、`NameError`(未定义变量) 以及 `st.pie_chart` 渲染错误反馈给 AI，由 AI 实时定位问题并给出修复补丁。
![alt text](data/AI3.png)
![alt text](data/AI4.png)

3.  **优化 阶段**: 设计streamlit的页面布局，优化用户交互流程。
![alt text](data/AI5.png)



### Prompt 设计
* **核心功能请求**：“我需要一个发票识别系统，功能要求：上传图片、提取开票单位/金额/税率、数据统计分析，功能越丰富越好。”
* **错误修复请求**：“运行后提示 `NameError: name 'tab2' is not defined`，请帮我解决，并优化绘图逻辑以防崩溃。”
* **实战优化请求**：“我需要可以实际投入使用的代码，增加图像增强功能以提升识别率，并加入人工校验机制。”

### 遇到问题与解决
1.  **环境兼容性问题**：运行 `pip install` 时崩溃。AI 建议升级 pip 并逐行安装，解决了 Python 3.13 下的安装冲突。
2.  **变量作用域错误**：在使用 `st.tabs` 时未定义变量。AI 重新整理了代码结构，确保所有 UI 组件都在正确的逻辑分支内。
3.  **数据格式报错**：绘图时由于 OCR 返回的数据类型不一致导致崩溃。AI 引入了 `pd.to_numeric` 进行强制转换，并改用更稳健的 `vega_lite_chart`。

---

## （4）核心代码说明

### 1. 图像增强逻辑
```python
def preprocess_image(image):
    gray_image = ImageOps.grayscale(image) # 灰度处理
    enhancer = ImageEnhance.Contrast(gray_image)
    gray_image = enhancer.enhance(1.8)     # 增强对比度提升文字清晰度
    return gray_image

```

### 2. 信息提取（正则匹配）

```python
# 提取金额：寻找包含“金额/合计/￥”等关键词后的数字
amount_match = re.findall(r"(?:金额|合计|￥|¥)\s*[:：]?\s*([0-9]{1,3}(?:,[0-9]{3})*(?:\.[0-9]{2}))", text)
# 提取单位：识别包含“有限公司/中心/集团”的字符串
unit_keywords = r"([^\s\n]*(?:有限公司|股份公司|中心|集团))"

```

### 3. 数据校验与联动

使用 `st.data_editor` 实现 UI 上的实时编辑，修改后的数据会自动同步到后续的统计图表中，体现了“人机共治”的财务理念。

---

## （5）运行说明

### 环境配置

1. **安装 Python 依赖**：
```bash
pip install streamlit pandas Pillow pytesseract

```


2. **安装 OCR 软件**：
* 下载并安装 `Tesseract-OCR`。
* **关键点**：安装时必须勾选 `Chinese (Simplified)` 语言包。
* 在代码中配置 `tesseract_cmd` 路径。



### 启动方式

在项目根目录下打开终端，执行：

```bash
streamlit run app.py

```

---

## （6）效果展示

### 1. 系统主界面

![alt text](data/image1.png)

### 2. 识别与校对明细

![alt text](data/image2.png)

### 3. 财务可视化看板

![alt text](data/image3.png)

### 4. 报表导出

![alt text](data/image4.png)